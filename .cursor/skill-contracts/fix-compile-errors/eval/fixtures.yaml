schema_version: 1
command: fix-compile-errors
ship_gate: [A]
cases:
  A1:
    rubric: "required"
    pass_anchor: "SKILL.md"
    pass_must_reference_skill: true
    skill_required:
      - "minimal diff"
      - "force a green build"
  A2:
    rubric: "required"
    pass_must_reference_skill: false
