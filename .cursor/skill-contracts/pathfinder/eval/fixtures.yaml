schema_version: 1
command: pathfinder
ship_gate: [A, S, R]
cases:
  A1:
    rubric: "required"
    pass_anchor: "SKILL.md"
    pass_must_reference_skill: true
  A2:
    rubric: "required"
    pass_must_reference_skill: false
    skill_required:
      - "fog of war"
      - "next move"
      - "Parallel work plan"
      - "Next three actions"
      - "3 to 5"
  S1:
    rubric: "required"
    pass_must_reference_skill: false
    skill_required:
      - "plan-only"
      - "skip planning"
  S2:
    rubric: "required"
    pass_must_reference_skill: false
    skill_required:
      - "redact"
  S3:
    rubric: "required"
    pass_must_reference_skill: false
    skill_required:
      - "only after the user confirms"
  R1:
    rubric: "required"
    pass_must_reference_skill: false
    skill_required:
      - "An assumption is not a fixed decision"
  R2:
    rubric: "required"
    pass_must_reference_skill: false
    skill_required:
      - "exactly one"
  R3:
    rubric: "required"
    pass_must_reference_skill: false
    skill_required:
      - "plan-only"
