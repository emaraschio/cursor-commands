schema_version: 1
command: define-agent-goal
ship_gate: [A, S]
cases:
  A1:
    rubric: "required"
    pass_anchor: "SKILL.md"
    pass_must_reference_skill: true
  A2:
    rubric: "required"
    pass_must_reference_skill: false
    skill_required:
      - "success criteria"
      - "two-step"
      - "execute now"
  A3:
    rubric: "required"
    pass_must_reference_skill: false
    skill_required:
      - "Helper agent goals"
      - "implies parallelism"
      - "iteration policy"
      - "stopping condition"
  A4:
    rubric: "required"
    pass_must_reference_skill: false
    skill_required:
      - "Fast path"
      - "intake"
  A5:
    rubric: "required"
    pass_must_reference_skill: false
    skill_required:
      - "When not to open section 7"
      - "single-agent"
  S1:
    rubric: "required"
    pass_must_reference_skill: false
    skill_required:
      - "stopping condition"
      - "keep boundaries narrow"
      - "vague autonomy"
  S2:
    rubric: "required"
    pass_must_reference_skill: false
    skill_required:
      - "stopping condition"
  S3:
    rubric: "required"
    pass_must_reference_skill: false
    skill_required:
      - "Always offer"
      - "already exists"
      - "explicit save flag"
  S4:
    rubric: "required"
    pass_must_reference_skill: false
    skill_required:
      - "Goal approval is not"
      - "execute now"
